function SectionTitle({ title }) {
  return (
    <h1 className="uppercase border-b-2 border-gray-200 py-3 w-[35%] text-[1.30rem] font-bold whitespace-nowrap">
      {title}
    </h1>
  );
}

export default SectionTitle;
